name: Codex PR Review Disabled

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Explain why this workflow is disabled
        run: |
          echo "PR reviews should now be triggered through the native Codex GitHub integration."
          echo "Use @codex review and enable automatic reviews in Codex settings."
