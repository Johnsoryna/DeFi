name: Codex Continuous Audit Disabled

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Explain why this workflow is disabled
        run: |
          echo "This repository now uses the documented Codex Cloud + GitHub integration path instead of the GitHub Action path."
          echo "See CODEX_CLOUD_SETUP.md and AGENTS.md."
